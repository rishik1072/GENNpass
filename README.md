# GENNpass
Secure Password Generator (GUI) A Python-based desktop application built using Tkinter that generates strong, customizable passwords. The application allows users to select password length and character types (uppercase, lowercase, digits, symbols), includes input validation, and supports one-click clipboard copying for usability.
